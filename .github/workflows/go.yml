name: Go

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2

    - name: Set up Go
      uses: actions/setup-go@v2
      with:
        go-version: 1.14.1
        
    - name: Fetch Source
      run: go get -insecure -u "github.com/golang/protobuf/proto"@v1.4.3
    
    - name: Fetch SourceV2Ray
      run: go get -insecure -u "github.com/v2ray/v2ray-core/app/router"@v4.23.2
      
    - name: Fetch Source Conf
      run: go get -insecure -u "github.com/v2ray/v2ray-core/infra/conf"@v4.23.2
      
    - name: Build
      run: go build -v ./..
      
    - name: GenerateRun
      run: ./V2SiteIP
